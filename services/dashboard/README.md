# dashboard — Control Plane UI (Next.js)

Placeholder. Built in ROADMAP Phase 5. Next.js app over the control-plane APIs:
policy authoring, audit viewer, decision-signature verification UI. Consumes the
shared TypeScript types generated from `/contracts`.
